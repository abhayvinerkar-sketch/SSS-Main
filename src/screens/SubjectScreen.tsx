import React from 'react';import{ScrollView,View,Text,StyleSheet,TouchableOpacity}from'react-native';import{subjects,materials}from'../data';import{getTopics}from'../data/topics';import{useTheme}from'../theme/ThemeProvider';
export function SubjectScreen({route,navigation}:any){const{colors}=useTheme();const subject=subjects.find(x=>x.id===route.params.id);if(!subject)return null;return <ScrollView style={[s.bg,{backgroundColor:colors.background}]} contentContainerStyle={s.pad}><Text style={[s.kicker,{color:colors.primary}]}>SUBJECT HUB</Text><Text style={[s.h,{color:colors.text}]}>{subject.name}</Text><Text style={[s.sub,{color:colors.muted}]}>{subject.chapters.length} chapters • topic-wise learning & practice</Text>{subject.chapters.map(chapter=>{const topics=getTopics(subject.id,chapter.id,chapter.title);const qCount=materials.some(m=>m.chapterId===chapter.id&&m.type==='Question Bank'&&m.available);return <View key={chapter.id} style={[s.chapter,{backgroundColor:colors.surface,borderColor:colors.border}]}><Text style={[s.chapterNo,{color:colors.primary}]}>CHAPTER {chapter.number}</Text><Text style={[s.title,{color:colors.text}]}>{chapter.title}</Text><Text style={[s.desc,{color:colors.muted}]}>{chapter.description}</Text><Text style={[s.topicHead,{color:colors.text}]}>Topics</Text><View style={s.topicWrap}>{topics.map(topic=><TouchableOpacity key={topic.id} onPress={()=>qCount&&navigation.navigate('QuestionBank',{subjectId:subject.id,chapterId:chapter.id,topic:topic.title})} style={[s.topic,{backgroundColor:colors.background,borderColor:colors.border}]}><Text style={[s.topicText,{color:colors.text}]}>{topic.title}</Text><Text style={[s.topicKind,{color:colors.primary}]}>{topic.kind}</Text></TouchableOpacity>)}</View><TouchableOpacity onPress={()=>navigation.navigate('ChapterReader',{subjectId:subject.id,chapterId:chapter.id})} style={[s.learn,{backgroundColor:colors.primary}]}><Text style={s.learnText}>📖 Learn chapter</Text></TouchableOpacity><TouchableOpacity disabled={!qCount} onPress={()=>navigation.navigate('QuestionBank',{subjectId:subject.id,chapterId:chapter.id})} style={[s.qb,{borderColor:colors.primary,opacity:qCount?1:.45}]}><Text style={[s.qbText,{color:colors.primary}]}>❓ Open full Question Bank</Text></TouchableOpacity></View>})}<View style={[s.note,{backgroundColor:colors.surface,borderColor:colors.border}]}><Text style={[s.noteText,{color:colors.muted}]}>Topic cards are the learning map for each chapter. Question-bank content is original practice material and can be expanded topic by topic.</Text></View></ScrollView>}
const s=StyleSheet.create({bg:{flex:1},pad:{padding:20,paddingBottom:40},kicker:{fontSize:11,fontWeight:'900',letterSpacing:1.2},h:{fontSize:30,fontWeight:'900',marginTop:5},sub:{fontSize:13,marginTop:4},chapter:{borderRadius:20,padding:17,marginTop:15,borderWidth:1},chapterNo:{fontSize:10,fontWeight:'900',letterSpacing:1},title:{fontSize:18,fontWeight:'900',marginTop:5},desc:{fontSize:12,lineHeight:18,marginTop:4,marginBottom:12},topicHead:{fontSize:14,fontWeight:'900',marginBottom:8},topicWrap:{gap:8},topic:{borderRadius:13,borderWidth:1,padding:11},topicText:{fontSize:13,fontWeight:'800'},topicKind:{fontSize:9,fontWeight:'900',marginTop:3,letterSpacing:.5},learn:{marginTop:12,padding:12,borderRadius:13,alignItems:'center'},learnText:{color:'#FFF',fontSize:12,fontWeight:'900'},qb:{marginTop:8,padding:11,borderRadius:13,borderWidth:1,alignItems:'center'},qbText:{fontSize:12,fontWeight:'900'},note:{marginTop:18,padding:15,borderRadius:15,borderWidth:1},noteText:{fontSize:12,lineHeight:19}});